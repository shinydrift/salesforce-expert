{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/schemas/generation/opportunity-bundle.schema.json",
  "title": "Opportunity Bundle — generation surface",
  "description": "An Opportunity with a generator line and an SLA line, priced from the standard pricebook. GENERATED by scripts/build-generation-schema.mjs from the YAML template — do not hand-edit. Run `node scripts/build-generation-schema.mjs` to refresh. Source: templates/opportunity-bundle.yaml.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "templateId",
    "parameters"
  ],
  "properties": {
    "templateId": {
      "const": "opportunity-bundle",
      "description": "The template this graph is expanded from."
    },
    "parameters": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "closeDate": {
          "type": "string",
          "format": "date"
        },
        "name": {
          "type": "string"
        },
        "quantity": {
          "type": "number",
          "default": 1
        }
      },
      "required": [
        "closeDate",
        "name"
      ]
    }
  }
}
