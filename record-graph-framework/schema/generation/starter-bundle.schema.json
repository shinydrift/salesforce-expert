{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/schemas/generation/starter-bundle.schema.json",
  "title": "Starter Bundle — generation surface",
  "description": "Core product plus standard support, single quote. GENERATED by scripts/build-generation-schema.mjs from the YAML template — do not hand-edit. Run `node scripts/build-generation-schema.mjs` to refresh. Source: templates/starter-bundle.yaml.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "templateId",
    "parameters"
  ],
  "properties": {
    "templateId": {
      "const": "starter-bundle",
      "description": "The template this graph is expanded from."
    },
    "parameters": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "opportunityId": {
          "type": "string"
        },
        "quantity": {
          "type": "number",
          "default": 1
        }
      },
      "required": [
        "opportunityId"
      ]
    }
  }
}
